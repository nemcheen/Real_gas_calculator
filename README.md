# Real Gas Calculator (Python) 🧪

Небольшой консольный проект на Python для расчёта параметров **реального газа** и построения 3D графика коэффициента сжимаемости Z(P,T).

## ✨ Возможности

- Выбор газа из списка с **реальными критическими параметрами**
- Расчёт всех параметров уравнения состояния (a, b, A, B, Z, V_real/V_ideal)
- **3D визуализация** зависимости Z от P и T
- Консольное меню с проверкой ввода


## 📁 Структура проекта

```
LabGasCalculator/
├── main.py          # Точка входа, главное меню
├── gas_params.py    # Словарь gases с параметрами газов
├── gas_io.py        # Функции ввода/проверок
├── plot.py          # 3D график Z(P, T)
├── calc.py          # Вычисления всех параметров (Z, a, b, A, B...)
└── requirements.txt # Зависимости
```


## 🧬 Параметры газов

```python
gases = {
    'metan': (190.6, 4.6e6, 0.011),        # метан CH₄
    'azot': (126.2, 3.39e6, 0.037),        # азот N₂
    'kislorod': (154.6, 5.04e6, 0.022),    # кислород O₂  
    'uglerod_oxid': (304.2, 7.39e6, 0.042), # CO₂
    'etan': (240.8, 6.14e6, 0.152),        # этан C₂H₆
}
```

**Формат:** `(T_c [K], P_c [Pa], ω [acentric factor])`

## 🚀 Быстрый старт

```bash
# Клонировать репозиторий
git clone https://github.com/<your-username>/LabGasCalculator.git
cd LabGasCalculator

# Установить зависимости
pip install -r requirements.txt

# Запустить
python main.py
```


## 🎮 Использование

```
Расчитать параметры реального газа: [C]
Вывести 3D график Z(P, T) [Z]  
Выход [E]
```

**Варианты:**

- **`C`** — ввести T, P, выбрать газ → получить все параметры
- **`Z`** — 3D график коэффициента сжимаемости
- **`E`** — выход


## 📊 Пример вывода

```
    Введено давление (Па): 1 000 000       (1.0 МПа ~ 10 атмосфер)  
    Введена температура (К): 300     (~27 °C)
    Газ: metan
    T критическая: 190.6 K   (~-83 °C)
    P критическое: 4600000.0 Па  (4.6 МПа ~ 45 атмосфер)
    ацентрический коэффициент ω = 0.011
    ----------------------------------------------------------
    РЕЗУЛЬТАТЫ РАСЧЕТА:

    Промежуточные параметры:
    a_c = 0.250
    k = 0.392
    α = 0.811
    a = 0.202
    b = 26.803 * 1e-6
    A = 0.033
    B = 0.011
    ----------------------------------------------------------
    Коэффициент сжимаемости, Z = 0.98
```


## 🔧 Зависимости

```
matplotlib==3.8.0
numpy==1.24.3
```


## 📝 Лицензия

[MIT](LICENSE) © 2026

***

**Проект для изучения уравнений состояния реальных газов** — идеально для студентов! 🎓
